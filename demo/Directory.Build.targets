<Project>

  <PropertyGroup Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net5.0'))">
    <DefineConstants>$(DefineConstants);HAS_IS_ASSIGNABLE_TO</DefineConstants>
  </PropertyGroup>

</Project>
